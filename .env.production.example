# 生产环境变量配置
# 部署到生产环境时，修改以下配置

# API 域名（部署后需要修改为实际的后端地址）
# 如果后端部署在同一个域名，可以使用相对路径
# 例如：https://your-domain.com
PROJECT_DOMAIN=https://your-domain.com

# 或者如果前后端分离，配置后端的完整地址
# PROJECT_DOMAIN=https://api.your-domain.com

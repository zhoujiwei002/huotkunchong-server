# 生产环境变量配置
# 部署到生产环境时，修改以下配置

# API 域名（部署后需要修改为实际的后端地址）
# 如果有后端服务，请修改为实际的域名
# 示例：https://api.your-domain.com
PROJECT_DOMAIN=https://your-api-domain.com

# 如果前后端在同一域名下，可以使用相对路径
# 例如：PROJECT_DOMAIN=/api

# 如果暂时没有后端服务，可以先不修改这个文件
# 在微信开发者工具中勾选"不校验合法域名"进行测试
